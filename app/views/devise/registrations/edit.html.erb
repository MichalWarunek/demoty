<div class="panel panel-default col-md-offset-3 col-md-6  ">
<div class="panel-heading"><h2>Edytuj konto</h2></div>
<div class="panel-body">


<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: {multipart: true, method: :put }) do |f| %>
  <%= devise_error_messages! %>

   <div class="form-group">
    <%= f.label :imię %><br />
    <%= f.text_field :name, autofocus: true, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, class: 'form-control' %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="form-group">
    <%= f.label :hasło %> <i>(zostaw puste pole, jeżeli nie chcesz zmieniać)</i><br />
    <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
    <% if @minimum_password_length %>
      <br />
      <em>Minimum <%= @minimum_password_length %> znaków</em>
    <% end %>
  </div>

  <div class="form-group">
    <%= f.label :potwierdzenie_hasła %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :obecne_hasło %> <i>(potrzebujemy Twojego obecnego hasła do zatwierdzenia zmian)</i><br />
    <%= f.password_field :current_password, autocomplete: "off", class: 'form-control' %>
  </div>

  <div class="form-group">
  <%= f.label :avatar %><br />
  <%= f.file_field :avatar %> <br />
  </div>

   <div class="actions">
    <%= f.submit "Zatwierdź", class: 'btn btn-primary' %>
    <%= link_to "Powrót", :back, class: 'btn btn-success'  %>
  </div>
<% end %>
</div>
<div class="panel-footer">
<h3>Usuwanie konta</h3>

<p>Brak satysfakcji? W każdej chwili możesz usunąć konto. <%= button_to "Usuń moje konto", registration_path(resource_name), data: { confirm: "Are you sure?"  }, method: :delete, class: 'btn btn-danger' %></p>


</div>
